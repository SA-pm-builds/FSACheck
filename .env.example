# FSACheck uses the Anthropic API client-side (browser).
# You enter your API key in the app's UI — it's stored in localStorage.
# No server-side environment variables are required to run this app.
#
# To get an API key: https://console.anthropic.com/settings/keys
